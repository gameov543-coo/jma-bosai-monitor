# 実装・検証記録

## 2026-09-14: 60秒監視と公開前検証

最新の既定監視間隔は60秒。以下の過去記録中の300秒は当時の設定である。

- 選定根拠: 気象庁の高頻度フィードは毎分更新。公開後の次回取得待ちを概ね0〜60秒に短縮し、毎時の長期取得・既読XMLの再取得防止を維持。
- `Config`の既定値、`.env.example`、ローカルの実稼働用`.env`を60秒に統一。60〜3600秒の入力境界と、60秒／300秒の周期（処理に10秒かかっても周期が累積しない）を自動テストで確認。
- ローカルPython 3.12.1で全43件成功。最初の制限環境ではローカルHTTPサーバーのポート確保を拒否された3件も、承認された実行環境で成功。
- Python 3.12のDockerイメージ内で全43件成功。`--network none`で外部通信を無効化し、HTTP契約テストはコンテナ内のループバックのみ使用。LINE/Slackの実宛先にテスト通知は送信していない。
- 秘密情報のない`.env.example`で別のComposeプロジェクトを初回起動し、気象庁の実データ取得とhealthyを確認。UID 10001、ルート読み取り専用、全capability削除、no-new-privileges、公開ポートなし、資格情報なしを検証。
- 検証用SQLiteに確認マーカーを保存し、コンテナ再作成後の保持とhealthy復帰を確認。検証用コンテナ・ボリュームは削除済み。
- 実稼働コンテナを60秒設定へ更新。更新前後とも送信済み26件、滞留・失敗0件。新規の重複送信はなく、healthyで復帰。
- MIT Licenseと第三者データの出典・加工内容・利用条件を追加。配布用Dockerイメージにもライセンスと出典を同梱し、存在を確認。
- GitHub ActionsにPython 3.12 / 3.13の全テスト、Docker内の全テスト、サンプル設定のCompose検証・地域解決を追加。
- Git履歴2コミットと作業ツリー計136ファイル相当について、実際のローカル通知資格情報との一致および代表的なトークン・秘密鍵パターンを確認し、検出0件。`.env`は権限0600で追跡対象外。DB・調査ファイルも公開対象外。
- READMEに取得手順、WindowsでのDocker / WSL2利用、監視間隔の根拠、別の環境設定ファイルによる検証方法を追記。

Slack実配送は資格情報がないため対象外。LINEは過去の端末受信確認と今回のHTTP契約テストを証拠とする。Macのスリープ時の監視停止、気象庁PULL型配信の遅延・停止、外部通知APIの配送条件は引き続きREADME記載の制約となる。実災害を発生させた試験や全災害形式への網羅保証ではない。

検証日: 2026-09-11（日本時間）。この記録は実行済みの結果を記録したものであり、現時点のホスト稼働を継続保証するものではない。

## 実行済みの確認

- Gitリポジトリを実装前に初期化した。`.env`は追跡対象外、権限0600。
- 気象庁公式Atom、XML、地域表、河川対応表を実際に取得した。
- 松本市・塩尻市・安曇野市が5警報区域に解決されることを確認した。
- その後ユーザーが設定した松本市・飯田市・諏訪市の公式情報取得・解析に成功した。
- Python標準ライブラリによる自動テスト33件を、実際のDockerイメージ内で成功させた。コンテナの外部ネットワークを無効化して実行し、HTTP契約テストはループバックだけを使用した。
- Slack: JSONペイロード、必要な通知項目、実HTTP送受信、成功応答の検証をテスト用サーバーで確認。実Slack宛先は未設定のためSlackワークスペースへの配送確認は未実施。
- LINE: Messaging APIへテスト通知を実送信しHTTP受付成功を確認。ユーザーから端末で受信できたとの回答を得た。
- Dockerイメージのビルド、Composeによる非root・読み取り専用ルート・名前付きボリュームでの起動に成功した。
- LINE通知を有効にして監視を起動した。初回は変化5件・解析エラー0件で、5件を送信済みにした。
- コンテナ再起動後、変化0件・解析エラー0件・送信済み5件のまま・滞留0件・healthcheck正常を確認した。状態と未送信キューが再起動をまたいで保持され、既に送った同じ情報の再通知は発生しなかった。

最後に確認した本番コンテナの結果:

```json
{"healthy":true,"last_success_unix":1789097218.367604,"outbox":{"sent":11},"active_alerts":1}
```

最終稼働イメージID: `sha256:b1ddc3dccde2b94c0cbd0fdbf48790fe7f0ad765a23c41f1ba3f0c2105a78836`。Docker context: `colima-bosai-check`。Compose project: `bosai`。サービス: `monitor`。本番DB: 名前付きボリューム`bosai_state`内の`/data/state.sqlite3`。

## 要件と証拠の対応

|要件|実装・確認|
|---|---|
|公式データの調査|docs/data-sources.md。2026年新体系・取得経路・利用条件・非採用候補を記載|
|定期監視|scheduler.py。5分間隔のスケジューラーテスト成功、常駐コンテナ起動済み|
|複数地域・都道府県・市区町村|areas.py。公式地域表、分割市域、河川対応テスト成功|
|状態変化と重要度|alert_parser.py、alert_evaluator.py。公式時系列fixtureで注意報→警報→危険警報・特別警報→下降→解除を確認|
|重複防止|state_store.py。既読URL・意味的状態差分・スナップショット時刻を永続化。再起動の実確認済み|
|Slack通知|notification_service.py。HTTP契約テスト成功、実ワークスペース配送は資格情報なしで未実施|
|LINE通知|notification_service.py。HTTP契約・再試行キーテスト成功、実送信と端末受信確認済み|
|責務分離・拡張|weather_provider、alert_parser、alert_evaluator、state_store、notification_service、scheduler、config|
|設定と秘密情報|.env.example、config.py、.gitignore。秘密情報をイメージ・ソースに含めない|
|SQLite永続化|トランザクションとoutbox、Docker名前付きボリューム、再起動実験|
|障害対応|タイムアウト、GETリトライ、通知バックオフ、429、恒久失敗、解析異常、古い電文のテスト|
|実災害なしの検証|tests/fixturesと3つのテストモジュール|
|24時間運用方法|READMEにVPS + Composeを選定し他方式と比較。Mac稼働中の常駐を確認。VPSへの配置は未実施|
|Docker|イメージ作成とCompose起動・再起動成功。最終差分の反映・ソース一致も確認済み|
|第三者向け手順|READMEに認証・地域・ローカル・Docker・テスト・デプロイ・バックアップ・復旧を記載|

## 最終変更と再検証

1. 通知待ちを確認する間隔を10秒から1秒に短縮した。情報取得周期は300秒のまま。`test_scheduler_polls_every_configured_interval`を追加して成功を確認。
2. 電文の「取消」で警報が解除されたと断定しないよう、解析段階で状態更新を止め、既存の取得・解析障害通知へ接続した。`test_cancelled_bulletin_never_means_all_clear`を追加して成功を確認。影響する公式fixtureテスト6件も再実行して成功。

テストは計33件。最終変更を含むイメージをComposeでビルドし、Docker内・外部ネットワーク無効の条件で33件すべて成功した（1.874秒）。そのイメージへサービスを更新し、コンテナ内のソースハッシュが以下の検証済みソースと一致することも確認した。

現ソース（bosai配下、__pycache__除外）のパスと内容を順に連結したSHA-256:
`5d5d24fbf02f8d16de68445958699af522015cc6c47696c976014b8cf2d76474`

## 最終稼働確認

途中で自動承認レビューの利用上限によりビルドが拒否されたが、案内された解除時刻後に同じ承認経路で再実行し成功した。制限を迂回する操作は行っていない。最終変更の未反映は解消済み。

- 更新前: healthy=true、送信済み11件、滞留・失敗0件、有効警報1件。
- 更新後: healthy=true、送信済み11件、滞留・失敗0件、有効警報1件。
- 更新直後の取得: changes=0、errors=0。コンテナ再作成による重複送信なし。
- 更新前には複数の連続した実ログでも約300秒間隔の監視を確認。
- 更新後はコンテナ内のソースSHA-256と作業ツリーが一致。

2026-09-11時点のユーザー設定は長野県の松本市・飯田市・諏訪市、監視間隔300秒、LINE通知有効。資格情報は`.env`だけに保存し、この記録には含めていない。Slackは実宛先が未設定のため、HTTP契約テストまでの確認である。LINEは実送信とユーザーによる端末受信確認まで完了している。

必要な実装・Docker反映・自動テスト・LINE受信確認に未完了事項はない。VPS購入やVPSへの実配置は行っていない。常時監視をMacで続けるにはMacとColimaの稼働が必要であり、スリープ・電源断中は監視できない。24時間稼働する本番環境への配置方法はREADMEのVPS手順を使用する。

## 2026-09-13: 飯田市への限定・警報時の見通し

ユーザーの依頼により、実稼働設定を飯田市（2020500）のみ、MIN_LEVEL=1（注意報以上）、300秒間隔、LINE通知有効に変更した。既存の送信履歴は保持した。

警報相当以上の状態変化通知に、公式VPWP50から今後約2時間に重なる3時間区間の予報を追加した。予報だけの変化は再通知しない。取得失敗でも警報通知を止めず、解除には以前の予報を添えない。

- 最新の公式飯田市予報を実際に取得し、時間帯・雨量・大雨／土砂災害危険度の表示を確認。
- 新しい予報テスト7件と、関連する状態保存・通知テスト11件、計18件を新Dockerイメージ内で実行し成功。全件再実行は行わず、変更の影響範囲を検証した。
- Dockerビルド・サービス再作成に成功。
- コンテナ内設定: areas=('長野県 飯田市',), minimum_level=1, interval=300, dry_run=False。
- 更新後healthcheck正常、送信済み24件・滞留0件・失敗0件。有効警報0件。更新前の送信済み24件から増加せず、更新による重複送信なし。

この追加変更でテストは計40件となった。予報は公式の3時間粒度を保持し、精密な2時間予報や2時間合計雨量を作ったとは扱わない。
